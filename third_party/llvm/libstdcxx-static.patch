diff --git a/3rd_party/gcc/gcc.BUILD.bazel b/3rd_party/gcc/gcc.BUILD.bazel
index 96ed646..673fbca 100644
--- a/3rd_party/gcc/gcc.BUILD.bazel
+++ b/3rd_party/gcc/gcc.BUILD.bazel
@@ -1699,6 +1699,14 @@
     ],
 )

+# rules_rust links native C++ dependencies into Rust binaries through the
+# toolchain's static runtime. Build that runtime from the same hermetic GCC
+# objects as the shared library instead of falling back to a host archive.
+cc_runtime_stage0_static_library(
+    name = "libstdc++.static",
+    deps = [":libstdcxx"],
+)
+
 # The shared library version and linker flags should stay aligned with GCC's
 # libstdc++-v3/acinclude.m4 shared library and symbol-version settings.
 cc_runtime_stage1_shared_library(
diff --git a/3rd_party/gcc/extension/trampoline.BUILD.bazel b/3rd_party/gcc/extension/trampoline.BUILD.bazel
index 6869e2d..31380d1 100644
--- a/3rd_party/gcc/extension/trampoline.BUILD.bazel
+++ b/3rd_party/gcc/extension/trampoline.BUILD.bazel
@@ -10,6 +10,7 @@ _SELECTED_FACADE_TARGETS = [
     "abi_headers_include_search_directory",
     "headers_include_search_directory",
     "libstdcxx.shared",
+    "libstdc++.static",
     "libstdcxxfs",
     "libstdcxxfs.static",
 ]
diff --git a/runtimes/cxxstdlib/BUILD.bazel b/runtimes/cxxstdlib/BUILD.bazel
index 819a51e..b7624fe 100644
--- a/runtimes/cxxstdlib/BUILD.bazel
+++ b/runtimes/cxxstdlib/BUILD.bazel
@@ -69,10 +69,9 @@ alias(
     actual = "//runtimes/libstdcxx:libstdcxx_shared_soname",
 )

-genrule(
+alias(
     name = "libstdcxx_static_runtime_lib",
-    outs = ["unsupported/libstdc++.a"],
-    cmd = "echo 'libstdc++ is only supported as a dynamic C++ runtime; static libstdc++ is intentionally unsupported.' >&2; exit 1",
+    actual = "//runtimes/libstdcxx:libstdc++.static",
 )

 genrule(
@@ -82,6 +81,9 @@ filegroup(
     name = "static_runtime_lib",
     srcs = select({
-        ":libstdcxx_linux": [":libstdcxx_static_runtime_lib"],
+        ":libstdcxx_linux": [
+            ":libstdcxx_static_runtime_lib",
+            "@llvm-project//libunwind:libunwind.static",
+        ],
         ":libstdcxx_windows": [":libstdcxx_windows_unsupported"],
         ":libcxx_linux": [
             "@llvm-project//libcxx:libcxx.static",
diff --git a/runtimes/libstdcxx/BUILD.bazel b/runtimes/libstdcxx/BUILD.bazel
index 9812905..568ea63 100644
--- a/runtimes/libstdcxx/BUILD.bazel
+++ b/runtimes/libstdcxx/BUILD.bazel
@@ -22,6 +22,12 @@
     visibility = ["//visibility:public"],
 )

+alias(
+    name = "libstdc++.static",
+    actual = "@gcc//:libstdc++.static",
+    visibility = ["//visibility:public"],
+)
+
 alias(
     name = "libstdcxx_shared_soname",
     actual = ":libstdcxx.shared",
